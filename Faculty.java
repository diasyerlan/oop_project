package proj;

import java.io.Serializable;
/**
 * The Faculty enum represents different faculties within an educational institution.
 * Each faculty is identified by a specific code.
 */
public enum Faculty implements Serializable {
	FIT, BS, KMA, SPE, ISE, SG
}
